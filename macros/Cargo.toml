[package]
name = "hummus-macros"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true # Important!

[dependencies]
syn = { version = "2.0", features = ["full"] } # Use syn 2.x
quote = "1.0"
proc-macro2 = "1.0"
